#include<conio.h>
#include<stdio.h>
#include<iostream>
using namespace std;

int main()
{
    int a=0,num,count,temp;

    cout<<"enter the no upto which you want to print armstrong no "<<endl;
    cin>>num;
    for(count=111;count<=num;count++)
    {
        temp=count;
        while(tep)
        {
            count=count/10;
            a++;
        }


    }
    return 0;
}

